{
  "name": "roxy-run-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The centralized leaderboard behind Roxy Run, as Vercel functions.",
  "scripts": {
    "dev": "vercel dev",
    "serve": "node --experimental-transform-types tools/serve.mjs",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
